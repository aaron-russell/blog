name: CI

on:
  pull_request:
  push:
    branches-ignore:
      - master

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify
        run: npm run verify

  build:
    runs-on: ubuntu-latest
    needs: verify
    env:
      CONTENTFUL_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_ACCESS_TOKEN }}
      CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
      CONTENTFUL_HOST: ${{ secrets.CONTENTFUL_HOST }}
      CF_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CF_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_AI_GATEWAY_ID: ${{ secrets.CLOUDFLARE_AI_GATEWAY_ID }}
    steps:
      - name: Check Contentful secrets
        id: contentful
        shell: bash
        run: |
          if [ -n "$CONTENTFUL_ACCESS_TOKEN" ] && [ -n "$CONTENTFUL_SPACE_ID" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Check AI summary secrets
        id: ai_summaries
        shell: bash
        run: |
          if [ -n "$CF_ACCOUNT_ID" ] && [ -n "$CF_API_TOKEN" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Check out repository
        if: steps.contentful.outputs.enabled == 'true'
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up Node.js
        if: steps.contentful.outputs.enabled == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        if: steps.contentful.outputs.enabled == 'true'
        run: npm ci

      - name: Generate AI summaries
        if: steps.contentful.outputs.enabled == 'true' && steps.ai_summaries.outputs.enabled == 'true'
        continue-on-error: true
        run: npm run ai:summaries:write

      - name: Note AI summary fallback
        if: steps.contentful.outputs.enabled == 'true' && steps.ai_summaries.outputs.enabled != 'true'
        run: echo "Cloudflare AI summary secrets not configured; build will use deterministic summary fallback."

      - name: Build
        if: steps.contentful.outputs.enabled == 'true'
        run: npm run build:contentful
