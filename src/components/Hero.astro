---
import ResponsiveImage from './ResponsiveImage.astro'
import styles from './hero.module.css'

type Props = {
  contentHtml?: string
  eyebrow?: string
  image?: any
  title: string
}

const { contentHtml, eyebrow, image, title } = Astro.props as Props
---

<div class={styles.hero}>
  {
    image ? (
      <ResponsiveImage
        asset={image}
        alt={title}
        class={styles.image}
        loading="eager"
        sizes="(min-width: 1024px) 68vw, 100vw"
        widths={[640, 960, 1280, 1600]}
      />
  ) : null
  }
  <div class={styles.glow} aria-hidden="true"></div>
  <div class={styles.details}>
    {eyebrow ? <span class="eyebrow">{eyebrow}</span> : null}
    <h1 class={styles.title}>{title}</h1>
    {contentHtml ? <div class={styles.content} set:html={contentHtml} /> : null}
  </div>
</div>
